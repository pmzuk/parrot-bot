useSyslog: false
nick: parrot
nickPassword: ""
ircAddress: irc.freenode.net
ircSSL: true
defaultEndpoint: default
httpAddress: :5555
httpURL: http://localhost:8080/
endpoints:
  default:
    target: "#chan1"
  grafana:
    target: "someNick"
    fmt: "{{.title}} {{.ruleUrl}}"

